name: Backend CI/CD

on:
  push:
    branches: ["main"]
    paths:
      - /backend/**
  pull_request:
    branches: ["main"]
    paths:
      - /backend/**

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Run a one-line script
        run: echo Hello, world!
